export { Predicate } from './types';

export {
    map, flatten, flatmap, find,
    filter, reduce, every, some,
    contains, chain, compact, compactObject,
} from './imodificaters';

export {
    avg, range, min, max, sum,
    enumerate, repeat, count, cycle,
} from './iaggregators';

export { iTake, take, takeWhile, dropWhile, chunked } from './icollectors'
